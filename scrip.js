document.addEventListener('DOMContentLoaded', function() {
    // Initialize AOS animations
    AOS.init({
        delay: 700,
        offset: 200,
        duration: 1000,
        once: true
    });
}); 
